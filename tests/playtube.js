'use strict';

describe('foo', function () {
    it('bar', function () {

    });
});

/*var chai = require('chai'),
    nock = require('nock'),
    expect = chai.expect;

describe('Playtube', function () {
    describe('when library is required multiple times', function () {
        it('iframe API is loaded once', function () {
            // require('./../src/playtube.js');
            // require('./../src/playtube.js');

            // ensure that only one request to load https://www.youtube.com/iframe_api has been made
        });
    });
});
*/
